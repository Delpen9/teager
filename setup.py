from setuptools import setup

setup(name = 'teager',
      version = '0.1',
      description = 'A module to cleanly run a Teager operation.',
      url = 'https://github.com/Delpen9/teager',
      author = 'Ian Dover',
      author_email = 'iantdover@gmail.com',
      license = 'MIT',
      packages = ['teager'],
      zip_safe = False)